# 阻抗等高線分析 - 開發、打包與發布說明

本文件記載本專案的日常開發、修改程式碼、一鍵打包產出 `.exe`，以及發布給他人使用的完整流程。

---

## 🛠️ 一、 日常開發與修改程式碼

在自己的電腦進行程式碼修改與測試時，**不需要每次都打包**。

1. **啟動開發環境**：
   - 雙擊根目錄的 **`start.bat`**（或於終端機執行 `npm run dev`）。
   - 瀏覽器會自動開啟 `http://localhost:5173/`。
2. **即時熱重載（Hot Reload）**：
   - 在 `src/` 資料夾內修改任何檔案（例如 `src/components/ContourPanel.vue`、`src/style.css` 等）並存檔，瀏覽器畫面會**即時自動更新**。

---

## 📦 二、 一鍵打包與發布流程（提供給別台電腦使用）

當功能修改完成，準備打包並提供給其他電腦使用時：

### 步驟 1：執行打包
* 雙擊專案根目錄的 **`build.bat`**。
* 打包腳本會自動完成以下工作：
  1. 編譯前端 Vue 靜態檔案到 `dist/` 資料夾。
  2. 自動調用 Windows 內建 C# 編譯器（`csc.exe`），將專屬圖示 `app.ico` 與啟動邏輯編譯成 **`dist\阻抗等高線分析.exe`**。

### 步驟 2：提供給對方
* 打包完成後，將整個 **`dist`** 資料夾壓縮成 `.zip` 壓縮檔（例如 `阻抗等高線分析_v1.0.zip`）。
* 將壓縮檔傳送給對方。

### 步驟 3：對方如何使用（免安裝）
1. 對方將 `.zip` 解壓縮到任意資料夾。
2. **直接雙擊 `阻抗等高線分析.exe`**。
3. 程式會自動在背景啟動服務並喚起預設瀏覽器開啟工具。
   * **特色 1**：完全不會出現 CMD 黑底視窗。
   * **特色 2**：Windows 右下角系統匣會出現專屬圖示，可點右鍵選單「開啟網頁」或「結束程式」。
   * **特色 3**：對方電腦不需要安裝 Node.js、Python 或任何額外軟體。

---

## 📂 三、 核心檔案結構與用途說明

| 檔案 / 資料夾 | 用途說明 |
| :--- | :--- |
| **`build.bat`** | **一鍵打包腳本**（雙擊即可自動完成網頁編譯與 `.exe` 生成） |
| **`build.ps1`** | 打包背後執行的 PowerShell 自動化腳本 |
| **`start.bat`** | 開發用啟動檔（快速啟動 `npm run dev` 開發伺服器） |
| **`app.ico`** | 內嵌進 `.exe` 啟動器的專屬 App 應用程式圖示 |
| **`launcher/Launcher.cs`** | C# 啟動器原始碼（負責無黑窗啟動、內嵌 HTTP 服務、系統匣選單、防止多開） |
| **`launcher/GenerateIcon.cs`**| 圖示生成工具源碼（可用於重新繪製與生成 `app.ico`） |
| **`dist/`** | **最終打包發布目錄**（直接將此資料夾壓縮寄給對方） |
| **`dist/阻抗等高線分析.exe`** | 帶有專屬圖示的免安裝綠色版執行檔 |

---

## 🔄 四、 更新程式碼至 GitHub 流程

若要將修改好的最新程式碼推送到 GitHub 儲存庫：

在終端機中依序執行以下指令：
```powershell
git add .
git commit -m "更新說明（請自訂）"
git push origin main
```
